public class VirtualMachine {
    // Virtual machine attributes and methods

    public void addRequest(Request request) {
        // Add the request to the VM
    }

    public double getUtilization() {
        // Calculate and return the VM's utilization
    }
}
